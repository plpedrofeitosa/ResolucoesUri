#include <iostream>

using namespace std;

int main()
{
    int q, x, qtd;

    cin >> q;
    
    qtd = 0;
    
    for (int i = 0; i < q; i++)
    {
        cin >> x;
        qtd += x;
    }

    if (qtd >= (q-qtd)) cout << "N\n";
    else cout << "Y\n";

    return 0;
}
